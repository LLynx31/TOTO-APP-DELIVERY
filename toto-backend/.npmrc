# pnpm configuration

# Use shamefully-hoist to fix potential issues with nested dependencies
# (makes node_modules structure flatter, similar to npm)
shamefully-hoist=true

# Auto-install peers
auto-install-peers=true

# Strict peer dependencies (set to false for flexibility)
strict-peer-dependencies=false

# Store directory (optional, uses default if not set)
# store-dir=~/.pnpm-store
