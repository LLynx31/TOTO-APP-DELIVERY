import 'package:flutter/material.dart';

class CreateDeliveryWizard extends StatelessWidget {
  const CreateDeliveryWizard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nouvelle livraison')),
      body: const Center(child: Text('CreateDeliveryWizard - À implémenter')),
    );
  }
}
